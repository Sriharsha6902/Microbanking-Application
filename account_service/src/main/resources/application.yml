spring:
  application:
    name: account-service

  config:
    import: "optional:configserver:"
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-service
      username: admin
      password: harsha123
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:postgresql://localhost:5432/microbank?account
    hikari:
      driver-class-name: org.postgresql.Driver
    username: postgres
    password: ${POSTGRES_PASS}
server:
  port: 8081

snowflake:
  node-id: 3001