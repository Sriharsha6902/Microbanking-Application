global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'microbanking_app'
    metrics_path: '/actuator/prometheus'
    consul_sd_configs:
      - server: 'localhost:8500'
        services: [ ]
    relabel_configs:
      - source_labels: [ __meta_consul_service ]
        target_label: job